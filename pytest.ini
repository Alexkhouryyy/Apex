[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    integration: requires ANTHROPIC_API_KEY and makes live API calls
    slow: boots Apex in a subprocess (~30s); the smoke suite
